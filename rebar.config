{erl_opts, [debug_info]}.

{escript_name, demo}.
{escript_emu_args, "%%! -escript main demo\n"}.
{escript_incl_apps, [map_sets, getopt]}.

{deps, [ {map_sets, "0.1.1"}
       , {getopt,   "1.0.1"} %% TODO: Get rid of it
       ]}.
