%% -*- mode:erlang -*-
{erl_opts, [debug_info]}.

{escript_name, demo}.
{escript_emu_args, "%%! -escript main demo\n"}.
{escript_incl_apps, [map_sets]}.

{deps, [ {map_sets, "1.1.0"}
       ]}.

{profiles, [{demo,
             [{src_dirs, ["src", "doc/example"]}]
            }]}.
